.env
auth-profile-secrets/*

# Runtime state created by OpenClaw. Keep only the checked-in sample config and
# workspace seed files.
config/**
!config/
!config/openclaw.json
!config/workspace-bbot/
!config/workspace-bbot/AGENTS.md
